# Development-only environment variables for Trace AI (do not commit real keys)
# Copy this file to .env.local and fill in your Gemini API key.
# Vite will expose variables prefixed with VITE_ to the client.

VITE_GEMINI_API_KEY=
VITE_GOOGLE_MAPS_API_KEY=